const numbers=[i=5,n=4]
function countDuplicate(numbers){
    let counts={}
    for (let i=0;i<number.length;i++){
        if(counts[numbers[i]]){
            counts[a[i]]=1
        }
    }
for(let prop in counts){
    if (counts[prop]>=2){
        console.log(prop +"counted:"+counts[props] +"times.")
    }
}
console.log(counts)     
}
countDuplicate(numbers)